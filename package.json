{
  "name": "order-system",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --remote",
    "dev:local": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute order-system --local --file=./schema.sql",
    "db:remote": "wrangler d1 execute order-system --remote --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^4.7.10"
  },
  "devDependencies": {
    "wrangler": "^4.19.1"
  }
}
